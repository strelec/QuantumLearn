package qlearn.ml.classify

/*import qlearn.util.nnet.Layer
import qlearn.util.nnet.activations.ActivationFunction
import qlearn.dataset.{MultiLabeled, SingleLabeled}
import qlearn.ml.Model


case class NeuralNetwork[T <: SingleLabeled[T]](
	layers: Vector[Layer],
	lastLevelActivation: ActivationFunction
) extends Model[MultiLabeled[T]] {

	def fit(data: MultiLabeled[T])= {
		FittedNeuralNetwork(data, 10)
	}
}
*/